//----> FUNCTION & PARAMETER <---- //

function hitungTotalBengkel(hargaSparepart) {
    const biayaPendaftaran = 10000;
    let total = biayaPendaftaran + hargaSparepart;
    console.log(`Total bayar ${biayaPendaftaran} + ${hargaSparepart} adalah: Rp${total}`);
}
hitungTotalBengkel(60000);


